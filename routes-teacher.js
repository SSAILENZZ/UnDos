const express=require('express');const {pool,activeYear}=require('./db');const {apiError,auth,requireRole}=require('./auth');const r=express.Router();r.use(auth,requireRole('teacher'));
async function getAssignment(req,id){const {rows}=await pool.query(`SELECT ta.*,s.name subject_name,c.name course_name,ay.year FROM teaching_assignments ta JOIN subjects s ON s.id=ta.subject_id JOIN courses c ON c.id=ta.course_id JOIN academic_years ay ON ay.id=ta.academic_year_id WHERE ta.id=$1 AND ta.teacher_id=$2 AND ta.active=TRUE`,[id,req.user.id]);return rows[0]||null}
r.get('/assignments',async(req,res)=>{try{const y=await activeYear(),{rows}=await pool.query(`SELECT ta.id,s.name subject_name,c.name course_name,(SELECT COUNT(*)::int FROM enrollments e WHERE e.course_id=c.id AND e.academic_year_id=$2) student_count,(SELECT COUNT(*)::int FROM evaluations ev WHERE ev.assignment_id=ta.id) evaluation_count FROM teaching_assignments ta JOIN subjects s ON s.id=ta.subject_id JOIN courses c ON c.id=ta.course_id WHERE ta.teacher_id=$1 AND ta.academic_year_id=$2 AND ta.active=TRUE ORDER BY c.level_order,c.name,s.name`,[req.user.id,y.id]);res.json({activeYear:y,assignments:rows.map(x=>({id:x.id,subjectName:x.subject_name,courseName:x.course_name,studentCount:x.student_count,evaluationCount:x.evaluation_count}))})}catch(e){console.error(e);apiError(res,500,'No se pudieron cargar tus cursos')}});
r.get('/assignments/:id',async(req,res)=>{try{const a=await getAssignment(req,Number(req.params.id));if(!a)return apiError(res,404,'Clase no encontrada');const [students,evals,grades]=await Promise.all([pool.query(`SELECT u.id,u.rut,u.full_name FROM enrollments e JOIN users u ON u.id=e.student_id WHERE e.course_id=$1 AND e.academic_year_id=$2 AND u.active=TRUE ORDER BY u.full_name`,[a.course_id,a.academic_year_id]),pool.query('SELECT id,name,eval_date,semester,weight::float,status FROM evaluations WHERE assignment_id=$1 ORDER BY semester,eval_date NULLS LAST,id',[a.id]),pool.query('SELECT g.evaluation_id,g.student_id,g.grade::float FROM grades g JOIN evaluations ev ON ev.id=g.evaluation_id WHERE ev.assignment_id=$1',[a.id])]);const coverage={1:0,2:0};for(const ev of evals.rows)if(ev.status==='completed')coverage[ev.semester]+=Number(ev.weight);res.json({assignment:{id:a.id,subjectName:a.subject_name,courseName:a.course_name,year:a.year},students:students.rows.map(x=>({id:x.id,rut:x.rut,fullName:x.full_name})),evaluations:evals.rows.map(x=>({id:x.id,name:x.name,date:x.eval_date,semester:x.semester,weight:Number(x.weight),status:x.status})),grades:grades.rows.map(x=>({evaluationId:x.evaluation_id,studentId:x.student_id,grade:Number(x.grade)})),completedWeight:coverage})}catch(e){console.error(e);apiError(res,500,'No se pudo cargar el libro de clases')}});
r.post('/assignments/:id/evaluations',async(req,res)=>{try{const a=await getAssignment(req,Number(req.params.id));if(!a)return apiError(res,404,'Clase no encontrada');const name=String(req.body.name||'').trim(),semester=Number(req.body.semester),weight=Number(req.body.weight),status=req.body.status==='completed'?'completed':'pending',date=req.body.date||null;if(!name||![1,2].includes(semester)||!(weight>0&&weight<=100))return apiError(res,400,'Datos de evaluación inválidos');const q=await pool.query('SELECT COALESCE(SUM(weight),0)::float total FROM evaluations WHERE assignment_id=$1 AND semester=$2',[a.id,semester]);if(Number(q.rows[0].total)+weight>100.001)return apiError(res,400,'La ponderación total del semestre no puede superar 100%');const {rows}=await pool.query('INSERT INTO evaluations(assignment_id,name,eval_date,semester,weight,status) VALUES($1,$2,$3,$4,$5,$6) RETURNING id,name,eval_date,semester,weight::float,status',[a.id,name,date,semester,weight,status]);res.status(201).json(rows[0])}catch(e){console.error(e);apiError(res,500,'No se pudo crear la evaluación')}});
r.patch('/evaluations/:id',async(req,res)=>{try{const id=Number(req.params.id),q=await pool.query(`SELECT ev.* FROM evaluations ev JOIN teaching_assignments ta ON ta.id=ev.assignment_id WHERE ev.id=$1 AND ta.teacher_id=$2`,[id,req.user.id]),old=q.rows[0];if(!old)return apiError(res,404,'Evaluación no encontrada');const name=req.body.name!==undefined?String(req.body.name).trim():old.name,semester=req.body.semester!==undefined?Number(req.body.semester):old.semester,weight=req.body.weight!==undefined?Number(req.body.weight):Number(old.weight),status=req.body.status!==undefined?req.body.status:old.status,date=req.body.date!==undefined?(req.body.date||null):old.eval_date;if(!name||![1,2].includes(semester)||!(weight>0&&weight<=100)||!['pending','completed'].includes(status))return apiError(res,400,'Datos de evaluación inválidos');const sum=await pool.query('SELECT COALESCE(SUM(weight),0)::float total FROM evaluations WHERE assignment_id=$1 AND semester=$2 AND id<>$3',[old.assignment_id,semester,id]);if(Number(sum.rows[0].total)+weight>100.001)return apiError(res,400,'La ponderación total del semestre no puede superar 100%');const {rows}=await pool.query('UPDATE evaluations SET name=$1,eval_date=$2,semester=$3,weight=$4,status=$5,updated_at=NOW() WHERE id=$6 RETURNING id,name,eval_date,semester,weight::float,status',[name,date,semester,weight,status,id]);res.json(rows[0])}catch(e){console.error(e);apiError(res,500,'No se pudo actualizar la evaluación')}});
r.post('/evaluations/:id/grades',async(req,res)=>{const c=await pool.connect();try{const id=Number(req.params.id),q=await c.query(`SELECT ta.course_id,ta.academic_year_id FROM evaluations ev JOIN teaching_assignments ta ON ta.id=ev.assignment_id WHERE ev.id=$1 AND ta.teacher_id=$2`,[id,req.user.id]),ev=q.rows[0];if(!ev)return apiError(res,404,'Evaluación no encontrada');const grades=Array.isArray(req.body.grades)?req.body.grades:[];await c.query('BEGIN');for(const item of grades){const studentId=Number(item.studentId),en=await c.query('SELECT 1 FROM enrollments WHERE student_id=$1 AND course_id=$2 AND academic_year_id=$3',[studentId,ev.course_id,ev.academic_year_id]);if(!en.rows[0])throw new Error('Estudiante fuera del curso');if(item.grade===null||item.grade===''||item.grade===undefined)await c.query('DELETE FROM grades WHERE evaluation_id=$1 AND student_id=$2',[id,studentId]);else{const g=Number(item.grade);if(!Number.isFinite(g)||g<2||g>7||Math.round(g*10)!==g*10)throw new Error('Nota inválida');await c.query('INSERT INTO grades(evaluation_id,student_id,grade) VALUES($1,$2,$3) ON CONFLICT(evaluation_id,student_id) DO UPDATE SET grade=EXCLUDED.grade,updated_at=NOW()',[id,studentId,g])}}await c.query('COMMIT');res.json({ok:true})}catch(e){await c.query('ROLLBACK').catch(()=>{});console.error(e);apiError(res,400,e.message==='Nota inválida'?'Las notas deben estar entre 2.0 y 7.0 con un decimal':'No se pudieron guardar las notas')}finally{c.release()}});
module.exports=r;
