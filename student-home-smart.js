(()=>{
const U=window.U,$=U.$,S=U.state;
const pad=n=>String(n).padStart(2,'0');
const schoolParts=(date=new Date())=>{const parts=new Intl.DateTimeFormat('en-US',{timeZone:'America/Santiago',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(date),get=t=>parts.find(p=>p.type===t)?.value;return {year:Number(get('year')),month:Number(get('month')),day:Number(get('day'))}};
const ymd=p=>`${p.year}-${pad(p.month)}-${pad(p.day)}`;
const schoolToday=()=>ymd(schoolParts());
const schoolMonth=()=>{const p=schoolParts();return `${p.year}-${pad(p.month)}`};
const plusDays=n=>{const p=schoolParts(),d=new Date(Date.UTC(p.year,p.month-1,p.day+n));return d.toISOString().slice(0,10)};
const daysUntil=date=>{const [y,m,d]=String(date||'').split('-').map(Number),p=schoolParts();if(!y||!m||!d)return 0;return Math.max(0,Math.round((Date.UTC(y,m-1,d)-Date.UTC(p.year,p.month-1,p.day))/86400000))};
const dayText=n=>n===0?'Hoy':n===1?'Mañana':`En ${n} días`;

const style=document.createElement('style');style.textContent=`
.student-smart-home{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(270px,.65fr);gap:14px;margin:-4px 0 20px}.student-smart-card{background:var(--card);border:1px solid var(--line);border-top:3px solid var(--orange);border-radius:17px;box-shadow:var(--shadow-soft);overflow:hidden}.student-smart-main{padding:18px}.student-smart-main-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;margin-bottom:13px}.student-smart-main-head h2{font-size:17px;margin:0 0 4px}.student-smart-main-head p{font-size:11px;color:var(--muted);margin:0}.student-smart-next{display:flex;align-items:center;gap:14px;padding:14px;background:linear-gradient(135deg,var(--surface-soft),var(--card));border:1px solid var(--line);border-radius:13px}.student-smart-date{width:64px;min-width:64px;text-align:center;border-radius:12px;background:var(--orange-soft);padding:9px 6px;color:var(--orange)}.student-smart-date strong{display:block;font-size:19px;line-height:1}.student-smart-date small{display:block;font-size:9px;font-weight:850;margin-top:4px;text-transform:uppercase}.student-smart-next-copy{min-width:0;flex:1}.student-smart-next-copy strong{display:block;font-size:14px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.student-smart-next-copy small{display:block;color:var(--muted);font-size:10px;margin-top:4px}.student-smart-next-copy .when{color:var(--orange);font-weight:850}.student-smart-empty{padding:15px;border:1px dashed var(--line);border-radius:13px;color:var(--muted);font-size:12px}.student-smart-side{padding:17px}.student-smart-side h2{margin:0 0 12px;font-size:16px}.student-smart-mini-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.student-smart-mini{padding:12px;border-radius:12px;background:var(--surface-soft);border:1px solid var(--line)}.student-smart-mini span{display:block;color:var(--muted);font-size:9px;font-weight:800}.student-smart-mini strong{display:block;margin-top:5px;font-size:18px;color:var(--text)}.student-smart-mini.att strong{color:var(--green)}.student-smart-mini.week strong{color:var(--orange)}.student-smart-actions{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.student-smart-action{border:1px solid var(--line);background:var(--card);color:var(--text);border-radius:11px;padding:9px 7px;font-size:10px;font-weight:800;cursor:pointer}.student-smart-action:hover{border-color:rgba(235,82,31,.45);background:var(--orange-soft)}.student-smart-list{display:grid;gap:8px;margin-top:11px}.student-smart-row{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px 10px;border:1px solid var(--line);border-radius:11px;background:var(--surface-soft)}.student-smart-row div{min-width:0}.student-smart-row strong{font-size:11px;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.student-smart-row small{display:block;color:var(--muted);font-size:9px;margin-top:2px}.student-smart-row .date{font-size:9px;color:var(--orange);font-weight:850;white-space:nowrap}.student-smart-preview-note{font-size:9px;color:var(--muted);margin-top:9px}.student-smart-card.loading{min-height:120px;display:grid;place-items:center;color:var(--muted);font-size:11px}
html[data-theme="dark"] .student-smart-card,html[data-theme="dark"] .student-smart-action{background:var(--card)!important;border-color:var(--line)!important}html[data-theme="dark"] .student-smart-next{background:linear-gradient(135deg,var(--surface-soft),var(--card))!important}
@media(max-width:1050px){.student-smart-home{grid-template-columns:1fr}}@media(max-width:650px){.student-smart-main-head{flex-direction:column}.student-smart-actions{grid-template-columns:1fr}.student-smart-mini-grid{grid-template-columns:1fr 1fr}.student-smart-next{align-items:flex-start;flex-wrap:wrap}.student-smart-next .btn{margin-left:78px}}
`;document.head.appendChild(style);

function nextMonth(m){const d=new Date(`${m}-01T00:00:00Z`);d.setUTCMonth(d.getUTCMonth()+1);return `${d.getUTCFullYear()}-${pad(d.getUTCMonth()+1)}`}
function sampleData(){const mk=(offset,name,subject,weight)=>({id:offset,name,date:plusDays(offset),subjectName:subject,teacherName:'Profesor de ejemplo',weight,status:'pending'});return {attendance:{summary:{percentage:91.4,present:32,absent:3,total:35,days:22}},evaluations:[mk(2,'Prueba de funciones','Matemáticas',30),mk(5,'Ensayo argumentativo','Lengua y Literatura',25),mk(8,'Control de unidad','Historia',20),mk(13,'Prueba de energía','Física',30)]}}
function recentGrades(){const d=S.student;if(!d?.subjects)return[];const rows=[];for(const s of d.subjects)for(const e of s.evaluations||[])if(e.grade!=null)rows.push({name:e.name,subjectName:s.name,date:e.date,grade:Number(e.grade)});return rows.sort((a,b)=>(b.date||'').localeCompare(a.date||'')).slice(0,3)}
function normalizeEvals(...calendars){const map=new Map();for(const c of calendars)for(const e of c?.evaluations||[])map.set(e.id,e);return [...map.values()].filter(e=>e.date&&e.date>=schoolToday()).sort((a,b)=>a.date.localeCompare(b.date)||String(a.subjectName).localeCompare(String(b.subjectName)))}
async function loadData(preview=false){
  if(preview&&S.adminPreview?.sample)return sampleData();
  const now=schoolMonth(),next=nextMonth(now),id=S.adminPreview?.user?.id;
  const prefix=preview?`/api/admin/preview/student/${id}`:'/api/student';
  const [att,c1,c2]=await Promise.allSettled([U.api(`${prefix}/attendance`),U.api(`${prefix}/calendar?month=${now}`),U.api(`${prefix}/calendar?month=${next}`)]);
  return {attendance:att.status==='fulfilled'?att.value:{summary:{}},evaluations:normalizeEvals(c1.status==='fulfilled'?c1.value:null,c2.status==='fulfilled'?c2.value:null)};
}
function renderBlock(data,preview=false){
  const head=$('.student-head');if(!head||$('#studentSmartHome'))return;
  const evals=data.evaluations||[],next=evals[0],weekEnd=plusDays(7),thisWeek=evals.filter(e=>e.date<=weekEnd).length,attendance=data.attendance?.summary?.percentage,grades=preview?[]:recentGrades();
  const box=document.createElement('div');box.id='studentSmartHome';box.className='student-smart-home';
  const nextDate=next?new Date(`${next.date}T00:00:00Z`):null;
  box.innerHTML=`<section class="student-smart-card student-smart-main"><div class="student-smart-main-head"><div><span class="eyebrow">Lo más importante</span><h2>Tu semana académica</h2><p>Lo próximo que necesitas tener en cuenta.</p></div>${thisWeek>=2?`<span class="badge warn">${thisWeek} evaluaciones en 7 días</span>`:'<span class="badge ok">Semana organizada</span>'}</div>${next?`<div class="student-smart-next"><div class="student-smart-date"><strong>${nextDate.getUTCDate()}</strong><small>${new Intl.DateTimeFormat('es-CL',{month:'short',timeZone:'UTC'}).format(nextDate)}</small></div><div class="student-smart-next-copy"><span class="when">${dayText(daysUntil(next.date))}</span><strong>${U.esc(next.name)}</strong><small>${U.esc(next.subjectName)} · ${next.weight??'—'}%${next.teacherName?` · Prof. ${U.esc(next.teacherName)}`:''}</small></div><button class="btn small secondary" data-smart-calendar>Ver calendario</button></div>`:'<div class="student-smart-empty">No tienes evaluaciones próximas registradas para este mes ni el siguiente.</div>'}${evals.length>1?`<div class="student-smart-list">${evals.slice(1,4).map(e=>`<div class="student-smart-row"><div><strong>${U.esc(e.name)}</strong><small>${U.esc(e.subjectName)}</small></div><span class="date">${U.fmtDate(e.date)}</span></div>`).join('')}</div>`:''}</section><aside class="student-smart-card student-smart-side"><h2>Resumen rápido</h2><div class="student-smart-mini-grid"><div class="student-smart-mini att"><span>Asistencia</span><strong>${attendance==null?'—':Number(attendance).toFixed(1)+'%'}</strong></div><div class="student-smart-mini week"><span>Próximos 7 días</span><strong>${thisWeek}</strong></div></div>${grades.length?`<div class="student-smart-list">${grades.map(g=>`<div class="student-smart-row"><div><strong>${U.esc(g.subjectName)}</strong><small>${U.esc(g.name)}</small></div><span class="date" style="font-size:15px">${g.grade.toFixed(1)}</span></div>`).join('')}</div>`:''}<div class="student-smart-actions"><button class="student-smart-action" data-smart-calendar>▣ Calendario</button><button class="student-smart-action" data-smart-attendance>✓ Asistencia</button><button class="student-smart-action" data-smart-subjects>▦ Materias</button></div>${preview?'<div class="student-smart-preview-note">Vista rápida del mismo inicio que verá el estudiante.</div>':''}</aside>`;
  head.insertAdjacentElement('afterend',box);
  box.addEventListener('click',e=>{if(e.target.closest('[data-smart-calendar]')){if(preview){const b=$('[data-preview-calendar]');if(b)b.click()}else U.navigate('student-calendar')}if(e.target.closest('[data-smart-attendance]')){if(preview){const b=$('[data-preview-attendance]');if(b)b.click()}else U.navigate('student-attendance')}if(e.target.closest('[data-smart-subjects]'))$('.student-section-head')?.scrollIntoView({behavior:'smooth',block:'start'})});
}
async function enhance(preview=false){if($('#studentSmartHome')||!$('.student-head'))return;try{renderBlock(await loadData(preview),preview)}catch(e){console.warn('No se pudo cargar el resumen inteligente',e)}}
const base=U.renderStudentHome;if(typeof base==='function')U.renderStudentHome=async()=>{await base();await enhance(false)};
const content=$('#content');if(content)new MutationObserver(()=>{if(S.user?.role==='admin'&&S.adminPreview?.role==='student')setTimeout(()=>enhance(true),0)}).observe(content,{childList:true,subtree:false});
})();
