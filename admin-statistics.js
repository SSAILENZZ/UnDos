(()=>{
const U=window.U,$=U.$,S=U.state;
const style=document.createElement('style');style.textContent=`
.admin-stats-head{display:flex;align-items:flex-end;justify-content:space-between;gap:16px;margin-bottom:18px}.admin-stats-head h1{margin:0 0 5px;font-size:30px;letter-spacing:-.8px}.admin-stats-head p{margin:0;color:var(--muted);max-width:760px}.admin-stats-main{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:14px}.admin-stat-card{position:relative;overflow:hidden;background:var(--card);border:1px solid var(--line);border-radius:16px;padding:17px 18px;box-shadow:var(--shadow-soft)}.admin-stat-card:before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:var(--orange)}.admin-stat-card.blue:before{background:var(--blue)}.admin-stat-card.green:before{background:var(--green)}.admin-stat-card span{display:block;color:var(--muted);font-size:11px;font-weight:750}.admin-stat-card strong{display:block;margin-top:6px;font-size:25px;color:var(--text)}.admin-stat-card small{display:block;margin-top:5px;color:var(--muted);font-size:10px;line-height:1.35}.admin-stats-mini{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-bottom:18px}.admin-mini{display:flex;justify-content:space-between;align-items:center;gap:10px;background:var(--surface-soft);border:1px solid var(--line);border-radius:13px;padding:11px 13px}.admin-mini span{color:var(--muted);font-size:10px;font-weight:750}.admin-mini b{color:var(--text);font-size:15px}.admin-stats-layout{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(290px,.65fr);gap:16px;align-items:start;margin-bottom:16px}.admin-stats-panel{background:var(--card);border:1px solid var(--line);border-top:3px solid var(--orange);border-radius:18px;overflow:hidden;box-shadow:var(--shadow-soft)}.admin-stats-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;padding:16px 17px;border-bottom:1px solid var(--line)}.admin-stats-panel-head h2{margin:0;font-size:17px}.admin-stats-panel-head p{margin:4px 0 0;color:var(--muted);font-size:11px}.admin-course-stat{display:grid;grid-template-columns:minmax(130px,1.2fr) 70px 80px minmax(120px,1fr) 80px;gap:10px;align-items:center;padding:13px 17px;border-bottom:1px solid var(--line)}.admin-course-stat:last-child{border-bottom:0}.admin-course-stat strong{font-size:12px}.admin-course-stat small{display:block;color:var(--muted);font-size:9px;margin-top:2px}.admin-course-num{font-size:11px;color:var(--text);font-weight:800}.admin-course-num span{display:block;color:var(--muted);font-weight:600;font-size:9px}.admin-progress{height:7px;background:var(--line);border-radius:999px;overflow:hidden;margin-top:5px}.admin-progress i{display:block;height:100%;background:linear-gradient(90deg,var(--blue),var(--green));border-radius:inherit}.admin-progress.orange i{background:linear-gradient(90deg,var(--orange),#f4a261)}.admin-health{display:grid;gap:12px;padding:16px}.admin-health-item{padding:12px;border-radius:13px;background:var(--surface-soft);border:1px solid var(--line)}.admin-health-top{display:flex;align-items:center;justify-content:space-between;gap:10px}.admin-health-top strong{font-size:11px}.admin-health-top b{font-size:13px}.admin-health-item p{margin:7px 0 0;color:var(--muted);font-size:10px;line-height:1.4}.admin-subject-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;padding:15px}.admin-subject-card{border:1px solid var(--line);border-radius:13px;padding:12px;background:var(--surface-soft)}.admin-subject-card strong{font-size:11px}.admin-subject-card .meta{display:flex;justify-content:space-between;gap:8px;margin-top:7px;color:var(--muted);font-size:9px}.admin-subject-card .grade{font-size:18px;color:var(--blue);font-weight:900;margin-top:5px}.admin-upcoming-list{display:grid}.admin-upcoming{display:grid;grid-template-columns:72px minmax(0,1fr) auto;gap:11px;align-items:center;padding:12px 16px;border-bottom:1px solid var(--line)}.admin-upcoming:last-child{border-bottom:0}.admin-upcoming-date{font-size:10px;font-weight:850;color:var(--orange)}.admin-upcoming strong{display:block;font-size:11px}.admin-upcoming small{display:block;color:var(--muted);font-size:9px;margin-top:3px}.admin-limit{background:var(--orange-soft);color:var(--orange);border-radius:999px;padding:5px 8px;font-size:9px;font-weight:850;white-space:nowrap}.admin-stats-empty{padding:30px 18px;text-align:center;color:var(--muted);font-size:11px}
html[data-theme="dark"] .admin-stat-card,html[data-theme="dark"] .admin-stats-panel{background:var(--card)!important;border-color:var(--line)!important}html[data-theme="dark"] .admin-mini,html[data-theme="dark"] .admin-health-item,html[data-theme="dark"] .admin-subject-card{background:var(--surface-soft)!important;border-color:var(--line)!important}
@media(max-width:1120px){.admin-stats-main,.admin-stats-mini{grid-template-columns:repeat(2,1fr)}.admin-stats-layout{grid-template-columns:1fr}}@media(max-width:760px){.admin-stats-head{align-items:flex-start;flex-direction:column}.admin-course-stat{grid-template-columns:1fr 70px 80px}.admin-course-stat .attendance-col,.admin-course-stat .grade-col{grid-column:auto}.admin-subject-grid{grid-template-columns:1fr}.admin-upcoming{grid-template-columns:62px 1fr}.admin-upcoming .admin-limit{grid-column:2}}@media(max-width:520px){.admin-stats-main,.admin-stats-mini{grid-template-columns:1fr}.admin-course-stat{grid-template-columns:1fr 1fr}.admin-course-stat>div:first-child{grid-column:1/-1}}
`;document.head.appendChild(style);

const pct=v=>v==null?'Sin datos':`${Number(v).toFixed(Number(v)%1?1:0)}%`;
const grade=v=>v==null?'—':Number(v).toFixed(1);
function percentage(a,b){return b?Math.round((a*1000/b))/10:null}

U.renderAdminStatistics=async()=>{
  const d=await U.api('/api/admin/statistics'),s=d.summary;S.adminStatistics=d;
  const completion=percentage(s.evaluationsCompleted,s.evaluations),coverage=percentage(s.enrollments,s.students),courseWithClasses=d.courses.filter(c=>c.assignments>0).length,courseCoverage=percentage(courseWithClasses,s.courses);
  const dayCounts=new Map();for(const e of d.upcoming){const k=`${e.courseId}|${e.date}`;dayCounts.set(k,(dayCounts.get(k)||0)+1)}
  U.setPage('Estadísticas',`Año escolar ${d.activeYear.year}`,'<button id="statsRefresh" class="btn secondary">↻ Actualizar</button>');
  document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.page==='admin-statistics'));
  $('#content').innerHTML=`
    <div class="admin-stats-head"><div><span class="eyebrow">Visión general</span><h1>Estadísticas administrativas</h1><p>Indicadores del año académico actual para revisar asistencia, evaluaciones, calificaciones y cobertura de los cursos.</p></div></div>
    <div class="admin-stats-main">
      <div class="admin-stat-card blue"><span>Estudiantes activos</span><strong>${s.students}</strong><small>${s.enrollments} con matrícula en el año actual</small></div>
      <div class="admin-stat-card green"><span>Asistencia registrada</span><strong>${pct(s.attendancePercentage)}</strong><small>${s.attendancePresent} presentes · ${s.attendanceAbsent} ausentes</small></div>
      <div class="admin-stat-card"><span>Evaluaciones</span><strong>${s.evaluations}</strong><small>${s.evaluationsCompleted} realizadas · ${s.evaluationsPending} pendientes</small></div>
      <div class="admin-stat-card blue"><span>Promedio de notas registradas</span><strong>${grade(s.gradesAverage)}</strong><small>${s.gradesCount} calificaciones ingresadas</small></div>
    </div>
    <div class="admin-stats-mini">
      <div class="admin-mini"><span>Profesores activos</span><b>${s.teachers}</b></div><div class="admin-mini"><span>Cursos activos</span><b>${s.courses}</b></div><div class="admin-mini"><span>Clases asignadas</span><b>${s.assignments}</b></div><div class="admin-mini"><span>Próximos 7 días</span><b>${s.evaluationsNext7} evaluaciones</b></div>
    </div>
    <div class="admin-stats-layout">
      <section class="admin-stats-panel"><div class="admin-stats-panel-head"><div><h2>Estado por curso</h2><p>Estudiantes, clases, evaluaciones, asistencia y promedio de calificaciones registradas.</p></div></div>${d.courses.length?d.courses.map(c=>`<div class="admin-course-stat"><div><strong>${U.esc(c.name)}</strong><small>${c.students} estudiantes</small></div><div class="admin-course-num">${c.assignments}<span>clases</span></div><div class="admin-course-num">${c.evaluations}<span>evaluaciones</span></div><div class="attendance-col"><div class="admin-course-num">${pct(c.attendancePercentage)}<span>asistencia</span></div><div class="admin-progress"><i style="width:${Math.max(0,Math.min(100,Number(c.attendancePercentage||0)))}%"></i></div></div><div class="grade-col admin-course-num">${grade(c.gradeAverage)}<span>promedio</span></div></div>`).join(''):'<div class="admin-stats-empty">No hay cursos activos.</div>'}</section>
      <aside class="admin-stats-panel"><div class="admin-stats-panel-head"><div><h2>Salud académica</h2><p>Indicadores rápidos para detectar información incompleta.</p></div></div><div class="admin-health">
        <div class="admin-health-item"><div class="admin-health-top"><strong>Matrícula del año</strong><b>${pct(coverage)}</b></div><div class="admin-progress"><i style="width:${Math.max(0,Math.min(100,Number(coverage||0)))}%"></i></div><p>Compara estudiantes activos con estudiantes matriculados en el año actual.</p></div>
        <div class="admin-health-item"><div class="admin-health-top"><strong>Cursos con clases asignadas</strong><b>${pct(courseCoverage)}</b></div><div class="admin-progress orange"><i style="width:${Math.max(0,Math.min(100,Number(courseCoverage||0)))}%"></i></div><p>${courseWithClasses} de ${s.courses} cursos tienen al menos una asignación docente.</p></div>
        <div class="admin-health-item"><div class="admin-health-top"><strong>Evaluaciones realizadas</strong><b>${pct(completion)}</b></div><div class="admin-progress"><i style="width:${Math.max(0,Math.min(100,Number(completion||0)))}%"></i></div><p>Proporción de evaluaciones marcadas como realizadas durante el año.</p></div>
        <div class="admin-health-item"><div class="admin-health-top"><strong>Días con asistencia</strong><b>${s.attendanceDays}</b></div><p>${s.attendanceTotal?`${s.attendanceTotal} registros de asistencia acumulados.`:'Todavía no existen registros de asistencia.'}</p></div>
      </div></aside>
    </div>
    <div class="admin-stats-layout">
      <section class="admin-stats-panel"><div class="admin-stats-panel-head"><div><h2>Asignaturas</h2><p>Actividad evaluativa y promedio de notas por materia.</p></div></div><div class="admin-subject-grid">${d.subjects.length?d.subjects.map(x=>`<div class="admin-subject-card"><strong>${U.esc(x.name)}</strong><div class="grade">${grade(x.gradeAverage)}</div><div class="meta"><span>${x.assignments} clases</span><span>${x.evaluations} evaluaciones</span><span>${x.pending} pendientes</span></div></div>`).join(''):'<div class="admin-stats-empty">Sin asignaturas activas.</div>'}</div></section>
      <aside class="admin-stats-panel"><div class="admin-stats-panel-head"><div><h2>Próximas evaluaciones</h2><p>Programación de los próximos 14 días.</p></div></div><div class="admin-upcoming-list">${d.upcoming.length?d.upcoming.slice(0,12).map(e=>{const n=dayCounts.get(`${e.courseId}|${e.date}`)||0;return `<div class="admin-upcoming"><div class="admin-upcoming-date">${U.fmtDate(e.date)}</div><div><strong>${U.esc(e.name)}</strong><small>${U.esc(e.courseName)} · ${U.esc(e.subjectName)} · ${U.esc(e.teacherName)}</small></div>${n>=2?'<span class="admin-limit">Día al límite</span>':''}</div>`}).join(''):'<div class="admin-stats-empty">No hay evaluaciones programadas durante los próximos 14 días.</div>'}</div></aside>
    </div>`;
  $('#statsRefresh').onclick=()=>U.renderAdminStatistics();
};

function ensureNav(){const nav=$('#nav');if(!nav||S.user?.role!=='admin'||S.adminPreview||nav.querySelector('[data-page="admin-statistics"]'))return;const b=document.createElement('button');b.type='button';b.className='nav-btn';b.dataset.page='admin-statistics';b.innerHTML='<span class="nav-icon">▥</span><span>Estadísticas</span>';const comm=nav.querySelector('[data-page="admin-communications"]'),hist=nav.querySelector('[data-page="admin-history"]');if(comm)comm.after(b);else nav.insertBefore(b,hist||null)}
const baseShell=U.renderShell;U.renderShell=()=>{baseShell();ensureNav()};
const baseNavigate=U.navigate;U.navigate=async page=>{if(page!=='admin-statistics')return baseNavigate(page);S.page=page;document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.page===page));$('#content').innerHTML='<div class="card empty">Cargando estadísticas…</div>';try{return await U.renderAdminStatistics()}catch(e){$('#content').innerHTML=`<div class="card"><h2>No se pudieron cargar las estadísticas</h2><p class="form-error">${U.esc(e.message)}</p><button id="retryAdminStats" class="btn ghost">Reintentar</button></div>`;$('#retryAdminStats').onclick=()=>U.navigate('admin-statistics')}};
const baseHome=U.renderAdminHome;if(typeof baseHome==='function')U.renderAdminHome=async()=>{await baseHome();const actions=$('#topActions');if(actions&&!$('#adminStatsTop'))actions.insertAdjacentHTML('afterbegin','<button id="adminStatsTop" class="btn secondary">▥ Estadísticas</button>');const b=$('#adminStatsTop');if(b)b.onclick=()=>U.navigate('admin-statistics')};
const nav=$('#nav');if(nav)new MutationObserver(()=>setTimeout(ensureNav,0)).observe(nav,{childList:true});setTimeout(ensureNav,0);
})();
