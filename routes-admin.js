const express=require('express');const bcrypt=require('bcryptjs');
const {pool,normalizeRut,validateRut,activeYear}=require('./db');const {apiError,auth,requireRole}=require('./auth');
const r=express.Router();r.use(auth,requireRole('admin'));
r.get('/overview',async(_q,res)=>{try{const y=await activeYear(),[years,courses,subjects,users,assignments]=await Promise.all([pool.query('SELECT id,year,active FROM academic_years ORDER BY year DESC'),pool.query('SELECT id,name,level_order,active FROM courses WHERE academic_year_id=$1 ORDER BY level_order,name',[y.id]),pool.query('SELECT id,name,active FROM subjects ORDER BY name'),pool.query(`SELECT u.id,u.rut,u.full_name,u.role,u.active,c.id course_id,c.name course_name FROM users u LEFT JOIN enrollments e ON e.student_id=u.id AND e.academic_year_id=$1 LEFT JOIN courses c ON c.id=e.course_id ORDER BY CASE u.role WHEN 'admin' THEN 1 WHEN 'teacher' THEN 2 ELSE 3 END,u.full_name`,[y.id]),pool.query(`SELECT ta.id,ta.teacher_id,u.full_name teacher_name,ta.subject_id,s.name subject_name,ta.course_id,c.name course_name,ta.active FROM teaching_assignments ta JOIN users u ON u.id=ta.teacher_id JOIN subjects s ON s.id=ta.subject_id JOIN courses c ON c.id=ta.course_id WHERE ta.academic_year_id=$1 ORDER BY c.level_order,c.name,s.name,u.full_name`,[y.id])]);res.json({activeYear:y,years:years.rows,courses:courses.rows,subjects:subjects.rows,users:users.rows.map(x=>({id:x.id,rut:x.rut,fullName:x.full_name,role:x.role,active:x.active,courseId:x.course_id,courseName:x.course_name})),assignments:assignments.rows.map(x=>({id:x.id,teacherId:x.teacher_id,teacherName:x.teacher_name,subjectId:x.subject_id,subjectName:x.subject_name,courseId:x.course_id,courseName:x.course_name,active:x.active}))})}catch(e){console.error(e);apiError(res,500,'No se pudo cargar el panel de administración')}});
r.post('/users',async(req,res)=>{const c=await pool.connect();try{const rut=normalizeRut(req.body.rut),fullName=String(req.body.fullName||'').trim(),role=req.body.role,password=String(req.body.password||''),courseId=req.body.courseId;if(!validateRut(rut))return apiError(res,400,'El RUT no es válido');if(!fullName)return apiError(res,400,'Falta el nombre');if(!['student','teacher','admin'].includes(role))return apiError(res,400,'Rol inválido');if(password.length<8)return apiError(res,400,'La contraseña debe tener al menos 8 caracteres');const hash=await bcrypt.hash(password,12);await c.query('BEGIN');const q=await c.query('INSERT INTO users(rut,full_name,password_hash,role) VALUES($1,$2,$3,$4) RETURNING id,rut,full_name,role,active',[rut,fullName,hash,role]),u=q.rows[0];if(role==='student'&&courseId){const y=await activeYear(c);await c.query('INSERT INTO enrollments(student_id,course_id,academic_year_id) VALUES($1,$2,$3)',[u.id,Number(courseId),y.id])}await c.query('COMMIT');res.status(201).json({id:u.id,rut:u.rut,fullName:u.full_name,role:u.role,active:u.active})}catch(e){await c.query('ROLLBACK').catch(()=>{});if(e.code==='23505')return apiError(res,409,'Ese RUT ya existe');console.error(e);apiError(res,500,'No se pudo crear el usuario')}finally{c.release()}});
r.patch('/users/:id',async(req,res)=>{try{const id=Number(req.params.id),q=await pool.query('SELECT * FROM users WHERE id=$1',[id]),old=q.rows[0];if(!old)return apiError(res,404,'Usuario no encontrado');const rut=req.body.rut!==undefined?normalizeRut(req.body.rut):old.rut,fullName=req.body.fullName!==undefined?String(req.body.fullName).trim():old.full_name,role=req.body.role!==undefined?req.body.role:old.role,active=req.body.active!==undefined?Boolean(req.body.active):old.active;if(!validateRut(rut)||!fullName||!['student','teacher','admin'].includes(role))return apiError(res,400,'Datos inválidos');if(id===req.user.id&&!active)return apiError(res,400,'No puedes desactivar tu propia cuenta');const {rows}=await pool.query('UPDATE users SET rut=$1,full_name=$2,role=$3,active=$4,updated_at=NOW() WHERE id=$5 RETURNING id,rut,full_name,role,active',[rut,fullName,role,active,id]),u=rows[0];res.json({id:u.id,rut:u.rut,fullName:u.full_name,role:u.role,active:u.active})}catch(e){if(e.code==='23505')return apiError(res,409,'Ese RUT ya existe');console.error(e);apiError(res,500,'No se pudo actualizar el usuario')}});
r.post('/users/:id/reset-password',async(req,res)=>{try{const password=String(req.body.password||'');if(password.length<8)return apiError(res,400,'La contraseña debe tener al menos 8 caracteres');const hash=await bcrypt.hash(password,12),q=await pool.query('UPDATE users SET password_hash=$1,updated_at=NOW() WHERE id=$2',[hash,Number(req.params.id)]);if(!q.rowCount)return apiError(res,404,'Usuario no encontrado');res.json({ok:true})}catch(e){console.error(e);apiError(res,500,'No se pudo cambiar la contraseña')}});
r.post('/enrollments',async(req,res)=>{try{const studentId=Number(req.body.studentId),courseId=Number(req.body.courseId),y=await activeYear(),s=await pool.query("SELECT id FROM users WHERE id=$1 AND role='student'",[studentId]);if(!s.rows[0])return apiError(res,400,'El usuario no es estudiante');await pool.query('INSERT INTO enrollments(student_id,course_id,academic_year_id) VALUES($1,$2,$3) ON CONFLICT(student_id,academic_year_id) DO UPDATE SET course_id=EXCLUDED.course_id',[studentId,courseId,y.id]);res.json({ok:true})}catch(e){console.error(e);apiError(res,500,'No se pudo asignar el curso')}});
r.post('/courses',async(req,res)=>{try{const name=String(req.body.name||'').trim(),levelOrder=Number(req.body.levelOrder||0),y=await activeYear();if(!name)return apiError(res,400,'Falta el nombre del curso');const {rows}=await pool.query('INSERT INTO courses(academic_year_id,name,level_order) VALUES($1,$2,$3) RETURNING id,name,level_order,active',[y.id,name,levelOrder]);res.status(201).json(rows[0])}catch(e){if(e.code==='23505')return apiError(res,409,'Ese curso ya existe este año');console.error(e);apiError(res,500,'No se pudo crear el curso')}});
r.post('/subjects',async(req,res)=>{try{const name=String(req.body.name||'').trim();if(!name)return apiError(res,400,'Falta el nombre de la asignatura');const {rows}=await pool.query('INSERT INTO subjects(name) VALUES($1) RETURNING id,name,active',[name]);res.status(201).json(rows[0])}catch(e){if(e.code==='23505')return apiError(res,409,'Esa asignatura ya existe');console.error(e);apiError(res,500,'No se pudo crear la asignatura')}});
r.post('/assignments',async(req,res)=>{try{const teacherId=Number(req.body.teacherId),subjectId=Number(req.body.subjectId),courseId=Number(req.body.courseId),y=await activeYear(),t=await pool.query("SELECT id FROM users WHERE id=$1 AND role='teacher' AND active=TRUE",[teacherId]);if(!t.rows[0])return apiError(res,400,'Profesor inválido');const {rows}=await pool.query('INSERT INTO teaching_assignments(teacher_id,subject_id,course_id,academic_year_id) VALUES($1,$2,$3,$4) RETURNING id',[teacherId,subjectId,courseId,y.id]);res.status(201).json({id:rows[0].id})}catch(e){if(e.code==='23505')return apiError(res,409,'Esa asignación ya existe');console.error(e);apiError(res,500,'No se pudo asignar la clase')}});
r.post('/years',async(req,res)=>{try{const year=Number(req.body.year);if(!Number.isInteger(year)||year<2020||year>2100)return apiError(res,400,'Año inválido');const {rows}=await pool.query('INSERT INTO academic_years(year,active) VALUES($1,FALSE) ON CONFLICT(year) DO UPDATE SET year=EXCLUDED.year RETURNING id,year,active',[year]);res.status(201).json(rows[0])}catch(e){console.error(e);apiError(res,500,'No se pudo crear el año académico')}});
r.post('/years/:id/activate',async(req,res)=>{const c=await pool.connect();try{await c.query('BEGIN');await c.query('UPDATE academic_years SET active=FALSE');const q=await c.query('UPDATE academic_years SET active=TRUE WHERE id=$1 RETURNING id,year',[Number(req.params.id)]);if(!q.rows[0])throw new Error('Año no encontrado');for(const [n,o] of [['7° Básico',7],['8° Básico',8],['1° Medio',9],['2° Medio',10],['3° Medio',11],['4° Medio',12]])await c.query('INSERT INTO courses(academic_year_id,name,level_order) VALUES($1,$2,$3) ON CONFLICT(academic_year_id,name) DO NOTHING',[q.rows[0].id,n,o]);await c.query('COMMIT');res.json({ok:true,activeYear:q.rows[0]})}catch(e){await c.query('ROLLBACK').catch(()=>{});console.error(e);apiError(res,500,'No se pudo activar el año académico')}finally{c.release()}});
module.exports=r;
