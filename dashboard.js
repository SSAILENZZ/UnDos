(()=>{
const U=window.U,$=U.$,S=U.state;
const style=document.createElement('style');style.textContent=`
.dash-head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;margin-bottom:17px}.dash-head h1{margin:0 0 5px;font-size:30px;letter-spacing:-.8px}.dash-head p{margin:0;color:var(--muted);max-width:760px}.dash-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:11px;margin-bottom:14px}.dash-stat{position:relative;overflow:hidden;background:var(--card);border:1px solid var(--line);border-radius:16px;padding:16px 17px;box-shadow:var(--shadow-soft)}.dash-stat:before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:var(--orange)}.dash-stat.blue:before{background:var(--blue)}.dash-stat.green:before{background:var(--green)}.dash-stat span{display:block;color:var(--muted);font-size:10px;font-weight:800}.dash-stat strong{display:block;margin-top:7px;font-size:24px;color:var(--text)}.dash-stat small{display:block;margin-top:4px;color:var(--muted);font-size:9px}.dash-grid{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(300px,.65fr);gap:14px;margin-bottom:14px;align-items:start}.dash-panel{background:var(--card);border:1px solid var(--line);border-top:3px solid var(--orange);border-radius:18px;overflow:hidden;box-shadow:var(--shadow-soft)}.dash-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;padding:15px 17px;border-bottom:1px solid var(--line)}.dash-panel-head h2{margin:0;font-size:16px}.dash-panel-head p{margin:4px 0 0;color:var(--muted);font-size:10px}.dash-chart{padding:16px 17px}.dash-svg{display:block;width:100%;height:210px;overflow:visible}.dash-axis{stroke:var(--line);stroke-width:1}.dash-line{fill:none;stroke:var(--blue);stroke-width:3;stroke-linecap:round;stroke-linejoin:round}.dash-area{fill:color-mix(in srgb,var(--blue) 14%,transparent)}.dash-point{fill:var(--card);stroke:var(--blue);stroke-width:2}.dash-label{fill:var(--muted);font-size:10px}.dash-value{fill:var(--text);font-size:10px;font-weight:800}.dash-empty{padding:34px 18px;text-align:center;color:var(--muted);font-size:10px}.dash-bars{display:grid;gap:10px;padding:16px}.dash-bar-row{display:grid;grid-template-columns:76px minmax(0,1fr) 34px;gap:9px;align-items:center}.dash-bar-row label{font-size:9px;color:var(--muted);font-weight:750}.dash-bar-track{height:10px;border-radius:999px;background:var(--line);overflow:hidden}.dash-bar-fill{height:100%;border-radius:inherit;background:linear-gradient(90deg,var(--blue),var(--green))}.dash-bar-fill.orange{background:linear-gradient(90deg,var(--orange),#f2a05f)}.dash-bar-row b{text-align:right;font-size:10px}.dash-donut-wrap{display:flex;align-items:center;justify-content:center;gap:18px;padding:21px 16px}.dash-donut{--p:0;width:124px;height:124px;border-radius:50%;display:grid;place-items:center;background:conic-gradient(var(--green) calc(var(--p)*1%),var(--line) 0);position:relative}.dash-donut:after{content:"";position:absolute;inset:12px;border-radius:50%;background:var(--card)}.dash-donut strong{position:relative;z-index:1;font-size:23px}.dash-donut-copy{display:grid;gap:8px}.dash-donut-copy span{font-size:9px;color:var(--muted)}.dash-donut-copy b{display:block;font-size:13px;color:var(--text)}.dash-list{display:grid}.dash-item{display:grid;grid-template-columns:70px minmax(0,1fr) auto;gap:10px;align-items:center;padding:11px 15px;border-bottom:1px solid var(--line)}.dash-item:last-child{border:0}.dash-date{font-size:9px;font-weight:850;color:var(--orange)}.dash-item strong{display:block;font-size:10px}.dash-item small{display:block;color:var(--muted);font-size:8px;margin-top:3px}.dash-course-list{display:grid;gap:9px;padding:14px}.dash-course{border:1px solid var(--line);border-radius:13px;background:var(--surface-soft);padding:11px 12px}.dash-course-top{display:flex;justify-content:space-between;gap:10px;align-items:center}.dash-course strong{font-size:10px}.dash-course small{display:block;color:var(--muted);font-size:8px;margin-top:2px}.dash-course-metrics{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:9px}.dash-course-metric{padding:8px;border-radius:9px;background:var(--card);border:1px solid var(--line)}.dash-course-metric span{display:block;color:var(--muted);font-size:7px}.dash-course-metric b{display:block;margin-top:3px;font-size:11px}.dash-subject{display:grid;grid-template-columns:minmax(120px,1fr) 82px minmax(100px,.8fr);gap:11px;align-items:center;padding:12px 15px;border-bottom:1px solid var(--line)}.dash-subject:last-child{border:0}.dash-subject strong{font-size:10px}.dash-subject small{display:block;color:var(--muted);font-size:8px;margin-top:3px}.dash-grade{font-size:13px;font-weight:900;color:var(--blue);text-align:center}.dash-mini-progress{height:7px;background:var(--line);border-radius:999px;overflow:hidden}.dash-mini-progress i{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,var(--orange),var(--green))}
html[data-theme="dark"] .dash-stat,html[data-theme="dark"] .dash-panel{background:var(--card)!important;border-color:var(--line)!important}html[data-theme="dark"] .dash-course{background:var(--surface-soft)!important;border-color:var(--line)!important}html[data-theme="dark"] .dash-course-metric{background:var(--card)!important;border-color:var(--line)!important}
@media(max-width:1080px){.dash-stats{grid-template-columns:repeat(2,1fr)}.dash-grid{grid-template-columns:1fr}}@media(max-width:650px){.dash-head{align-items:flex-start;flex-direction:column}.dash-stats{grid-template-columns:1fr 1fr}.dash-item{grid-template-columns:60px 1fr}.dash-item>span:last-child{grid-column:2}.dash-subject{grid-template-columns:1fr 70px}.dash-subject>.dash-mini-progress{grid-column:1/-1}}@media(max-width:430px){.dash-stats{grid-template-columns:1fr}.dash-donut-wrap{align-items:flex-start;flex-direction:column}.dash-svg{height:180px}}
`;document.head.appendChild(style);

const pct=v=>v==null?'—':`${Number(v).toFixed(Number(v)%1?1:0)}%`;
const grade=v=>v==null?'—':Number(v).toFixed(1);
const monthName=m=>{if(!m)return '';const [y,mo]=m.split('-');return new Intl.DateTimeFormat('es-CL',{month:'short',timeZone:'UTC'}).format(new Date(`${y}-${mo}-01T00:00:00Z`)).replace('.','')};
function lineChart(rows){if(!rows?.length)return '<div class="dash-empty">Todavía no hay suficientes registros para mostrar una tendencia.</div>';const data=rows.slice(-8),w=700,h=210,p=34,vals=data.map(x=>Number(x.percentage||0)),step=data.length>1?(w-p*2)/(data.length-1):0,pts=data.map((x,i)=>{const xx=p+i*step,yy=h-p-(Math.max(0,Math.min(100,Number(x.percentage||0)))/100)*(h-p*2);return {x:xx,y:yy,v:Number(x.percentage||0),m:x.month}}),poly=pts.map(x=>`${x.x},${x.y}`).join(' '),area=`${p},${h-p} ${poly} ${pts[pts.length-1].x},${h-p}`;return `<div class="dash-chart"><svg class="dash-svg" viewBox="0 0 ${w} ${h}" role="img" aria-label="Tendencia de asistencia"><line class="dash-axis" x1="${p}" y1="${h-p}" x2="${w-p}" y2="${h-p}"></line><line class="dash-axis" x1="${p}" y1="${p}" x2="${p}" y2="${h-p}"></line><text class="dash-label" x="4" y="${p+4}">100%</text><text class="dash-label" x="12" y="${h-p+4}">0%</text><polygon class="dash-area" points="${area}"></polygon><polyline class="dash-line" points="${poly}"></polyline>${pts.map(x=>`<circle class="dash-point" cx="${x.x}" cy="${x.y}" r="4"></circle><text class="dash-value" x="${x.x}" y="${Math.max(12,x.y-10)}" text-anchor="middle">${Math.round(x.v)}%</text><text class="dash-label" x="${x.x}" y="${h-9}" text-anchor="middle">${U.esc(monthName(x.m))}</text>`).join('')}</svg></div>`}
function bars(rows,{orange=false}={}){const max=Math.max(1,...(rows||[]).map(x=>Number(x.total||0)));return `<div class="dash-bars">${(rows||[]).map(x=>`<div class="dash-bar-row"><label>${U.esc(x.bucket)}</label><div class="dash-bar-track"><div class="dash-bar-fill ${orange?'orange':''}" style="width:${Number(x.total||0)*100/max}%"></div></div><b>${x.total}</b></div>`).join('')||'<div class="dash-empty">Sin notas registradas.</div>'}</div>`}
function stat(label,value,small='',cls=''){return `<div class="dash-stat ${cls}"><span>${label}</span><strong>${value}</strong>${small?`<small>${small}</small>`:''}</div>`}
function pageBase(d,subtitle){U.setPage('Dashboard',`Año escolar ${d.activeYear?.year||''}`,'<button id="dashRefresh" class="btn secondary">↻ Actualizar</button>');document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.page===`${d.role}-dashboard`));return `<div class="dash-head"><div><span class="eyebrow">Indicadores visuales</span><h1>Dashboard</h1><p>${subtitle}</p></div></div>`}
function upcoming(items,role){return `<div class="dash-list">${items?.length?items.map(e=>`<div class="dash-item"><span class="dash-date">${U.fmtDate(e.date)}</span><div><strong>${U.esc(e.name)}</strong><small>${U.esc(e.subjectName||'')}${e.courseName?` · ${U.esc(e.courseName)}`:''}${e.teacherName&&role==='student'?` · ${U.esc(e.teacherName)}`:''}</small></div><span class="badge ${role==='student'?'warn':'blue'}">${e.weight?`${Number(e.weight)}%`:'Próxima'}</span></div>`).join(''):'<div class="dash-empty">No hay evaluaciones próximas.</div>'}</div>`}
function renderAdmin(d){const s=d.summary,evalPct=s.evaluations?Math.round(s.completed*1000/s.evaluations)/10:null;$('#content').innerHTML=pageBase(d,'Una vista rápida del estado académico del liceo, con asistencia, calificaciones, evaluaciones y comparación entre cursos.')+`<div class="dash-stats">${stat('Estudiantes',s.students,`${s.enrollments} matriculados`,'blue')}${stat('Asistencia general',pct(s.attendancePercentage),`${s.attendanceTotal} registros`,'green')}${stat('Promedio de notas',grade(s.gradeAverage),'calificaciones registradas')}${stat('Evaluaciones realizadas',pct(evalPct),`${s.completed} de ${s.evaluations}`)}</div><div class="dash-grid"><section class="dash-panel"><div class="dash-panel-head"><div><h2>Tendencia de asistencia</h2><p>Evolución mensual del porcentaje de presentes.</p></div></div>${lineChart(d.monthlyAttendance)}</section><aside class="dash-panel"><div class="dash-panel-head"><div><h2>Avance evaluativo</h2><p>Evaluaciones marcadas como realizadas.</p></div></div><div class="dash-donut-wrap"><div class="dash-donut" style="--p:${evalPct||0}"><strong>${pct(evalPct)}</strong></div><div class="dash-donut-copy"><span>Realizadas<b>${s.completed}</b></span><span>Pendientes<b>${Math.max(0,s.evaluations-s.completed)}</b></span></div></div></aside></div><div class="dash-grid"><section class="dash-panel"><div class="dash-panel-head"><div><h2>Comparación por curso</h2><p>Asistencia y promedio de notas registradas.</p></div></div><div class="dash-course-list">${d.courses?.length?d.courses.map(c=>`<div class="dash-course"><div class="dash-course-top"><div><strong>${U.esc(c.name)}</strong><small>${c.students} estudiantes</small></div><span class="badge blue">${pct(c.attendancePercentage)} asistencia</span></div><div class="dash-course-metrics"><div class="dash-course-metric"><span>Promedio notas</span><b>${grade(c.gradeAverage)}</b></div><div class="dash-course-metric"><span>Asistencia</span><b>${pct(c.attendancePercentage)}</b></div></div></div>`).join(''):'<div class="dash-empty">Sin cursos.</div>'}</div></section><aside class="dash-panel"><div class="dash-panel-head"><div><h2>Distribución de notas</h2><p>Cantidad de notas por rango.</p></div></div>${bars(d.gradeDistribution,{orange:true})}</aside></div><div class="dash-panel"><div class="dash-panel-head"><div><h2>Próximas evaluaciones</h2><p>Programación de los próximos 14 días.</p></div></div>${upcoming(d.upcoming,'admin')}</div>`}
function renderTeacher(d){const s=d.summary,evalPct=s.evaluations?Math.round(s.completed*1000/s.evaluations)/10:null;$('#content').innerHTML=pageBase(d,'Resumen de tus cursos, asistencia, notas y evaluaciones programadas para tomar decisiones rápidas.')+`<div class="dash-stats">${stat('Clases asignadas',s.assignments,'cursos y materias','blue')}${stat('Asistencia',pct(s.attendancePercentage),'en tus clases','green')}${stat('Promedio de notas',grade(s.gradeAverage),'notas registradas')}${stat('Evaluaciones realizadas',pct(evalPct),`${s.completed} de ${s.evaluations}`)}</div><div class="dash-grid"><section class="dash-panel"><div class="dash-panel-head"><div><h2>Tendencia de asistencia</h2><p>Asistencia mensual de tus clases.</p></div></div>${lineChart(d.monthlyAttendance)}</section><aside class="dash-panel"><div class="dash-panel-head"><div><h2>Distribución de notas</h2><p>Notas registradas en todas tus clases.</p></div></div>${bars(d.gradeDistribution)}</aside></div><div class="dash-grid"><section class="dash-panel"><div class="dash-panel-head"><div><h2>Mis clases</h2><p>Comparación de rendimiento y asistencia.</p></div></div>${d.assignments?.length?d.assignments.map(a=>`<div class="dash-subject"><div><strong>${U.esc(a.courseName)} · ${U.esc(a.subjectName)}</strong><small>${a.students} estudiantes · ${a.evaluations} evaluaciones</small></div><div class="dash-grade">${grade(a.gradeAverage)}</div><div><div class="dash-mini-progress"><i style="width:${Math.max(0,Math.min(100,Number(a.attendancePercentage||0)))}%"></i></div><small>${pct(a.attendancePercentage)} asistencia</small></div></div>`).join(''):'<div class="dash-empty">No tienes clases asignadas.</div>'}</section><aside class="dash-panel"><div class="dash-panel-head"><div><h2>Próximas evaluaciones</h2><p>Próximos 14 días.</p></div></div>${upcoming(d.upcoming,'teacher')}</aside></div>`}
function renderStudent(d){const s=d.summary;$('#content').innerHTML=pageBase(d,'Tu rendimiento y asistencia resumidos con gráficos fáciles de revisar.')+`<div class="dash-stats">${stat('Curso',U.esc(d.course?.name||'—'),'año actual','blue')}${stat('Asistencia',pct(s.attendancePercentage),'registro acumulado','green')}${stat('Promedio de notas',grade(s.gradeAverage),`${s.grades||0} notas registradas`)}${stat('Evaluaciones próximas',s.upcoming,'próximos 14 días')}</div><div class="dash-grid"><section class="dash-panel"><div class="dash-panel-head"><div><h2>Mi asistencia por mes</h2><p>Cómo ha evolucionado tu asistencia durante el año.</p></div></div>${lineChart(d.monthlyAttendance)}</section><aside class="dash-panel"><div class="dash-panel-head"><div><h2>Distribución de mis notas</h2><p>Cantidad de calificaciones por rango.</p></div></div>${bars(d.gradeDistribution,{orange:true})}</aside></div><div class="dash-grid"><section class="dash-panel"><div class="dash-panel-head"><div><h2>Rendimiento por materia</h2><p>Promedio de notas registradas y asistencia por asignatura.</p></div></div>${d.subjects?.length?d.subjects.map(a=>{const gp=a.gradeAverage==null?0:Math.max(0,Math.min(100,(Number(a.gradeAverage)-2)*20));return `<div class="dash-subject"><div><strong>${U.esc(a.name)}</strong><small>Prof. ${U.esc(a.teacherName||'Por asignar')}</small></div><div class="dash-grade">${grade(a.gradeAverage)}</div><div><div class="dash-mini-progress"><i style="width:${gp}%"></i></div><small>${pct(a.attendancePercentage)} asistencia</small></div></div>`}).join(''):'<div class="dash-empty">No hay materias registradas.</div>'}</section><aside class="dash-panel"><div class="dash-panel-head"><div><h2>Próximas evaluaciones</h2><p>Próximos 14 días.</p></div></div>${upcoming(d.upcoming,'student')}</aside></div>`}

U.renderDashboard=async()=>{const d=await U.api('/api/dashboard');S.dashboard=d;if(d.role==='admin')renderAdmin(d);else if(d.role==='teacher')renderTeacher(d);else renderStudent(d);const r=$('#dashRefresh');if(r)r.onclick=()=>U.renderDashboard()};
function ensureNav(){if(!S.user||S.adminPreview)return;const nav=$('#nav');if(!nav)return;const page=`${S.user.role}-dashboard`;if(nav.querySelector(`[data-page="${page}"]`))return;const b=document.createElement('button');b.type='button';b.className='nav-btn';b.dataset.page=page;b.innerHTML='<span class="nav-icon">▥</span><span>Dashboard</span>';const home=nav.querySelector(`[data-page="${S.user.role}-home"]`);if(home)home.after(b);else nav.appendChild(b)}
const shellBase=U.renderShell;U.renderShell=()=>{shellBase();ensureNav()};
const navBase=U.navigate;U.navigate=async page=>{if(!['admin-dashboard','teacher-dashboard','student-dashboard'].includes(page))return navBase(page);S.page=page;document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.page===page));const c=$('#content');if(c)c.innerHTML='<div class="card empty">Cargando dashboard…</div>';try{return await U.renderDashboard()}catch(e){if(c)c.innerHTML=`<div class="card"><h2>No se pudo cargar el dashboard</h2><p class="form-error">${U.esc(e.message)}</p><button id="retryDashboard" class="btn ghost">Reintentar</button></div>`;const r=$('#retryDashboard');if(r)r.onclick=()=>U.navigate(page)}};
const nav=$('#nav');if(nav)new MutationObserver(()=>setTimeout(ensureNav,0)).observe(nav,{childList:true});document.addEventListener('click',()=>setTimeout(ensureNav,0),true);setTimeout(ensureNav,0);
})();