(()=>{
const U=window.U,$=U.$,S=U.state;
const style=document.createElement('style');style.textContent=`
.promo-head{display:flex;justify-content:space-between;align-items:flex-end;gap:16px;margin-bottom:18px}.promo-head h1{margin:0 0 5px;font-size:30px;letter-spacing:-.8px}.promo-head p{margin:0;color:var(--muted);max-width:780px}.promo-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:16px}.promo-stat{position:relative;overflow:hidden;background:var(--card);border:1px solid var(--line);border-radius:16px;padding:16px 17px;box-shadow:var(--shadow-soft)}.promo-stat:before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:var(--orange)}.promo-stat.blue:before{background:var(--blue)}.promo-stat.green:before{background:var(--green)}.promo-stat span{display:block;color:var(--muted);font-size:10px;font-weight:800}.promo-stat strong{display:block;margin-top:6px;font-size:24px;color:var(--text)}.promo-stat small{display:block;margin-top:5px;color:var(--muted);font-size:9px}.promo-prepare{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px;align-items:center;background:linear-gradient(110deg,var(--card),var(--orange-soft));border:1px solid rgba(235,82,31,.24);border-left:4px solid var(--orange);border-radius:17px;padding:18px;margin-bottom:16px}.promo-prepare h2{margin:0 0 5px;font-size:17px}.promo-prepare p{margin:0;color:var(--muted);font-size:11px;line-height:1.5}.promo-note{display:flex;gap:10px;align-items:flex-start;background:var(--surface-soft);border:1px solid var(--line);border-radius:13px;padding:12px 13px;margin-bottom:16px}.promo-note b{display:block;font-size:11px}.promo-note p{margin:3px 0 0;color:var(--muted);font-size:10px;line-height:1.45}.promo-toolbar{display:flex;gap:10px;align-items:center;justify-content:space-between;flex-wrap:wrap;padding:13px 14px;border-bottom:1px solid var(--line)}.promo-toolbar .search{min-width:240px;flex:1}.promo-panel{background:var(--card);border:1px solid var(--line);border-top:3px solid var(--orange);border-radius:18px;overflow:hidden;box-shadow:var(--shadow-soft)}.promo-groups{display:grid}.promo-group{border-bottom:1px solid var(--line)}.promo-group:last-child{border-bottom:0}.promo-group-head{display:flex;justify-content:space-between;align-items:center;gap:12px;background:var(--surface-soft);padding:11px 15px}.promo-group-head strong{font-size:12px}.promo-group-head span{color:var(--muted);font-size:9px}.promo-row{display:grid;grid-template-columns:36px minmax(190px,1.4fr) minmax(150px,.8fr) 34px minmax(180px,1fr) 130px;gap:10px;align-items:center;padding:12px 15px;border-top:1px solid var(--line)}.promo-row:hover{background:rgba(7,55,90,.035)}.promo-check{width:18px;height:18px;accent-color:var(--orange)}.promo-person{min-width:0}.promo-person strong{display:block;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.promo-person small{display:block;color:var(--muted);font-size:9px;margin-top:2px}.promo-course{font-size:10px;font-weight:750;color:var(--text)}.promo-arrow{text-align:center;color:var(--orange);font-weight:900}.promo-select{width:100%;min-width:0;border:1px solid var(--line);background:var(--card);color:var(--text);border-radius:9px;padding:8px 9px;font-size:10px}.promo-status{justify-self:end;text-align:right}.promo-status .badge{white-space:normal;text-align:center}.promo-empty{padding:34px 18px;text-align:center;color:var(--muted);font-size:11px}.promo-footer{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:14px;border-top:1px solid var(--line);background:var(--surface-soft)}.promo-footer small{color:var(--muted);font-size:9px}.promo-course-map{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;margin-bottom:16px}.promo-map-card{background:var(--card);border:1px solid var(--line);border-radius:13px;padding:11px 12px}.promo-map-card span{display:block;color:var(--muted);font-size:9px}.promo-map-card strong{display:block;margin-top:4px;font-size:11px}.promo-map-card i{color:var(--orange);font-style:normal;margin:0 5px}.promo-loading{padding:36px;text-align:center;color:var(--muted)}
html[data-theme="dark"] .promo-stat,html[data-theme="dark"] .promo-panel,html[data-theme="dark"] .promo-map-card{background:var(--card)!important;border-color:var(--line)!important}html[data-theme="dark"] .promo-prepare{background:linear-gradient(110deg,var(--card),#2a1b17)!important}html[data-theme="dark"] .promo-select{background:var(--surface-soft)!important;color:var(--text)!important}html[data-theme="dark"] .promo-row:hover{background:rgba(255,255,255,.025)}
@media(max-width:1100px){.promo-stats{grid-template-columns:repeat(2,1fr)}.promo-course-map{grid-template-columns:repeat(2,1fr)}.promo-row{grid-template-columns:32px minmax(170px,1.2fr) 120px 28px minmax(160px,1fr);}.promo-status{grid-column:2/-1;justify-self:start;text-align:left}}
@media(max-width:720px){.promo-head{align-items:flex-start;flex-direction:column}.promo-prepare{grid-template-columns:1fr}.promo-course-map,.promo-stats{grid-template-columns:1fr}.promo-row{grid-template-columns:30px 1fr}.promo-row>*{grid-column:2}.promo-row>.promo-check{grid-column:1;grid-row:1/5;align-self:start;margin-top:2px}.promo-arrow{display:none}.promo-status{justify-self:start}.promo-toolbar{align-items:stretch}.promo-toolbar .search{min-width:0;width:100%}.promo-footer{align-items:flex-start;flex-direction:column}}
`;document.head.appendChild(style);

const targetById=(d,id)=>d.targetCourses.find(c=>Number(c.id)===Number(id));
const rowsFiltered=(d,q)=>{q=String(q||'').trim().toLowerCase();return q?d.students.filter(s=>(`${s.fullName} ${U.fmtRut(s.rut)} ${s.sourceCourseName} ${s.targetCourseName||''}`).toLowerCase().includes(q)):d.students};

U.renderAdminPromotion=async()=>{
  const d=await U.api('/api/admin/promotion');S.adminPromotion=d;
  render(d,'');
};

function render(d,query=''){
  const promoted=d.students.filter(s=>s.targetEnrollmentId).length,graduating=d.students.filter(s=>s.graduating&&!s.targetEnrollmentId).length,pending=d.students.filter(s=>!s.targetEnrollmentId).length,auto=d.students.filter(s=>!s.targetEnrollmentId&&s.suggestedTargetCourseId).length;
  U.setPage('Promoción de estudiantes',`${d.sourceYear.year} → ${d.targetYearNumber}`,'<button id="promoRefresh" class="btn secondary">↻ Actualizar</button>');
  document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.page==='admin-promotion'));
  const filtered=rowsFiltered(d,query),groups=d.sourceCourses.map(c=>({course:c,students:filtered.filter(s=>s.sourceCourseId===c.id)})).filter(g=>g.students.length);
  const maps=d.sourceCourses.map(c=>{const sample=d.students.find(s=>s.sourceCourseId===c.id&&s.suggestedTargetCourseId),t=sample?targetById(d,sample.suggestedTargetCourseId):null;return `<div class="promo-map-card"><span>${U.esc(c.name)} · ${c.studentCount} estudiantes</span><strong>${U.esc(c.name)} <i>→</i> ${t?U.esc(t.name):(c.levelOrder>=12?'Egreso':'Seleccionar destino')}</strong></div>`}).join('');
  $('#content').innerHTML=`
    <div class="promo-head"><div><span class="eyebrow">Cambio de año escolar</span><h1>Promoción de estudiantes</h1><p>Prepara la matrícula del próximo año sin modificar las notas, asistencia ni historial del año actual.</p></div></div>
    <div class="promo-stats"><div class="promo-stat blue"><span>Estudiantes del año ${d.sourceYear.year}</span><strong>${d.students.length}</strong><small>Matrículas activas encontradas</small></div><div class="promo-stat green"><span>Ya matriculados en ${d.targetYearNumber}</span><strong>${promoted}</strong><small>No se vuelven a procesar</small></div><div class="promo-stat"><span>Pendientes de revisar</span><strong>${pending}</strong><small>${auto} con curso sugerido automáticamente</small></div><div class="promo-stat"><span>Posibles egresos</span><strong>${graduating}</strong><small>Estudiantes del último nivel sin matrícula futura</small></div></div>
    ${!d.targetYear||!d.targetCourses.length?`<div class="promo-prepare"><div><h2>Preparar año escolar ${d.targetYearNumber}</h2><p>${d.targetYear?'El año ya existe, pero necesita sus cursos.':'UnDos creará el año como inactivo y copiará la estructura de cursos actual.'} No se cambiará el año activo ni se borrará información de ${d.sourceYear.year}.</p></div><button id="preparePromotionYear" class="btn primary">Preparar ${d.targetYearNumber}</button></div>`:''}
    <div class="promo-note"><span class="nav-icon">i</span><div><b>Proceso seguro e independiente</b><p>Promover agrega una nueva matrícula para ${d.targetYearNumber}. La matrícula de ${d.sourceYear.year} permanece intacta. El próximo año solo se vuelve activo cuando tú lo actives desde “Años escolares”.</p></div></div>
    ${d.targetCourses.length?`<div class="promo-course-map">${maps}</div><section class="promo-panel"><div class="promo-toolbar"><input id="promoSearch" class="search" value="${U.esc(query)}" placeholder="Buscar estudiante, RUT o curso"><div class="group"><button id="promoSelectSuggested" class="btn small secondary">Seleccionar sugeridos</button><button id="promoClear" class="btn small ghost">Limpiar selección</button></div></div><div class="promo-groups">${groups.length?groups.map(g=>groupHtml(d,g.course,g.students)).join(''):'<div class="promo-empty">No hay estudiantes que coincidan con la búsqueda.</div>'}</div><div class="promo-footer"><small>Revisa el curso de destino antes de confirmar. Puedes elegir otro curso si un estudiante repite o cambia de sección.</small><button id="promoApply" class="btn primary">Promover seleccionados</button></div></section>`:'<div class="promo-empty card">Prepara el próximo año para comenzar.</div>'}`;
  bind(d,query);
}

function groupHtml(d,course,students){
  return `<div class="promo-group"><div class="promo-group-head"><strong>${U.esc(course.name)}</strong><span>${students.length} ${students.length===1?'estudiante':'estudiantes'}</span></div>${students.map(s=>rowHtml(d,s)).join('')}</div>`;
}
function rowHtml(d,s){
  const already=!!s.targetEnrollmentId,def=s.targetCourseId||s.suggestedTargetCourseId||'';
  return `<div class="promo-row" data-promo-row="${s.id}" data-search="${U.esc((s.fullName+' '+U.fmtRut(s.rut)+' '+s.sourceCourseName).toLowerCase())}">
    <input class="promo-check" type="checkbox" data-promo-check="${s.id}" ${already?'disabled':''} ${!already&&s.suggestedTargetCourseId?'checked':''} aria-label="Seleccionar ${U.esc(s.fullName)}">
    <div class="promo-person"><strong>${U.esc(s.fullName)}</strong><small>${U.esc(U.fmtRut(s.rut))}</small></div>
    <div class="promo-course">${U.esc(s.sourceCourseName)}</div><div class="promo-arrow">→</div>
    <select class="promo-select" data-promo-target="${s.id}" ${already?'disabled':''}><option value="">${s.graduating?'Egreso / sin matrícula':'Seleccionar curso'}</option>${d.targetCourses.map(c=>`<option value="${c.id}" ${Number(def)===Number(c.id)?'selected':''}>${U.esc(c.name)}</option>`).join('')}</select>
    <div class="promo-status">${already?`<span class="badge ok">Ya en ${U.esc(s.targetCourseName)}</span>`:s.graduating?'<span class="badge warn">Revisar egreso</span>':s.suggestedTargetCourseId?'<span class="badge blue">Sugerido</span>':'<span class="badge">Sin sugerencia</span>'}</div>
  </div>`;
}
function bind(d,query){
  const refresh=$('#promoRefresh');if(refresh)refresh.onclick=()=>U.renderAdminPromotion();
  const prepare=$('#preparePromotionYear');if(prepare)prepare.onclick=async()=>{const old=prepare.textContent;prepare.disabled=true;prepare.textContent='Preparando…';try{await U.api('/api/admin/promotion/prepare',{method:'POST',body:{year:d.targetYearNumber}});U.toast(`Año ${d.targetYearNumber} preparado`);await U.renderAdminPromotion()}catch(e){U.toast(e.message);prepare.disabled=false;prepare.textContent=old}};
  const search=$('#promoSearch');if(search)search.oninput=e=>render(d,e.target.value);
  const suggested=$('#promoSelectSuggested');if(suggested)suggested.onclick=()=>document.querySelectorAll('[data-promo-row]').forEach(row=>{const id=row.dataset.promoRow,s=d.students.find(x=>x.id===Number(id)),c=row.querySelector('[data-promo-check]'),sel=row.querySelector('[data-promo-target]');if(!s?.targetEnrollmentId&&s?.suggestedTargetCourseId&&c&&sel){c.checked=true;sel.value=String(s.suggestedTargetCourseId)}});
  const clear=$('#promoClear');if(clear)clear.onclick=()=>document.querySelectorAll('[data-promo-check]:not(:disabled)').forEach(c=>c.checked=false);
  const apply=$('#promoApply');if(apply)apply.onclick=()=>applyPromotion(d,apply);
}
async function applyPromotion(d,button){
  const items=[];document.querySelectorAll('[data-promo-check]:checked:not(:disabled)').forEach(c=>{const id=Number(c.dataset.promoCheck),sel=document.querySelector(`[data-promo-target="${id}"]`),targetCourseId=Number(sel?.value);if(targetCourseId)items.push({studentId:id,targetCourseId})});
  if(!items.length){U.toast('Selecciona estudiantes y un curso de destino');return}
  if(!confirm(`Se crearán ${items.length} matrículas para ${d.targetYearNumber}. El historial de ${d.sourceYear.year} no se modificará. ¿Continuar?`))return;
  const old=button.textContent;button.disabled=true;button.textContent='Promoviendo…';
  try{const result=await U.api('/api/admin/promotion/apply',{method:'POST',body:{items}});U.toast(`${result.promoted} estudiantes promovidos${result.already?` · ${result.already} ya estaban matriculados`:''}`);await U.renderAdminPromotion()}catch(e){U.toast(e.message);button.disabled=false;button.textContent=old}
}
function ensureNav(){const nav=$('#nav');if(!nav||S.user?.role!=='admin'||S.adminPreview||nav.querySelector('[data-page="admin-promotion"]'))return;const b=document.createElement('button');b.type='button';b.className='nav-btn';b.dataset.page='admin-promotion';b.innerHTML='<span class="nav-icon">↗</span><span>Promoción</span>';const stats=nav.querySelector('[data-page="admin-statistics"]'),hist=nav.querySelector('[data-page="admin-history"]');if(stats)stats.after(b);else nav.insertBefore(b,hist||null)}
const baseShell=U.renderShell;U.renderShell=()=>{baseShell();ensureNav()};
const baseNavigate=U.navigate;U.navigate=async page=>{if(page!=='admin-promotion')return baseNavigate(page);S.page=page;document.querySelectorAll('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.page===page));$('#content').innerHTML='<div class="promo-loading card">Cargando promoción…</div>';try{return await U.renderAdminPromotion()}catch(e){$('#content').innerHTML=`<div class="card"><h2>No se pudo cargar la promoción</h2><p class="form-error">${U.esc(e.message)}</p><button id="retryPromotion" class="btn ghost">Reintentar</button></div>`;$('#retryPromotion').onclick=()=>U.navigate('admin-promotion')}};
const baseYears=U.renderAdminYears;if(typeof baseYears==='function')U.renderAdminYears=async()=>{await baseYears();const actions=$('#topActions');if(actions&&!$('#adminPromotionFromYears'))actions.insertAdjacentHTML('afterbegin','<button id="adminPromotionFromYears" class="btn secondary">↗ Promover estudiantes</button>');const b=$('#adminPromotionFromYears');if(b)b.onclick=()=>U.navigate('admin-promotion')};
const baseHome=U.renderAdminHome;if(typeof baseHome==='function')U.renderAdminHome=async()=>{await baseHome();const card=document.querySelector('.admin-quick-card');if(card&&!card.querySelector('[data-admin-promotion-quick]'))card.insertAdjacentHTML('beforeend','<div class="quick-row" data-admin-promotion-quick><div><strong>Promoción de estudiantes</strong><small>Preparar la matrícula del próximo año sin perder historial</small></div><button class="btn small secondary" id="adminPromotionQuick">Abrir</button></div>');const b=$('#adminPromotionQuick');if(b)b.onclick=()=>U.navigate('admin-promotion')};
const nav=$('#nav');if(nav)new MutationObserver(()=>setTimeout(ensureNav,0)).observe(nav,{childList:true});setTimeout(ensureNav,0);
})();
